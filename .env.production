# Build-time environment variables for production.
# This file is committed to version control — DO NOT put secrets here.
# NEXT_PUBLIC_* vars are baked into the client bundle during `next build`.
# They must be present at build time, not just at Worker runtime.

NEXT_PUBLIC_SITE_URL=https://www.mhc-gc.com
