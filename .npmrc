legacy-peer-deps=true
# Hoist all packages to the root node_modules so packages/shared can resolve
# its transitive dependencies (react, clsx, @opennextjs/cloudflare, etc.)
# during TypeScript compilation by apps/website and apps/dashboard.
public-hoist-pattern[]=*
