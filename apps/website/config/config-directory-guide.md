# Config Directory Guide (Canonical Redirect)

The canonical configuration directory guide is maintained at:

- [config/config-directory-guide.md](../../../config/config-directory-guide.md)

This app-scoped file is a pointer to avoid duplicate maintenance and drift.
